import Table from '../Table/Table';
const Orders = () => {

    return (
        <div>
            // Oredrs Table with all the orders with service name, customer name, date, status,Service Provider Name,Total Bill, etc.
            <Table/>

            // Add a button to add a new order
        </div>
    )   


}
export default Orders;